<div class="comment">
  <h3>Comment <%= comment.id %>:</h3>
  <p><%= sanitize comment.body %>
    <%= sanitize comment.body,
        tags: %w(table tr td),
        attributes: %w(id class style) %></p>
  <div class="reply">
    <%= render comment.replies %>
  </div>
</div>
